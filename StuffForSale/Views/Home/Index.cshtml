<script type="text/javascript" src="js/index_page.js"></script>

<table class='table table-stripped' id="firstTable">
    <thead>
        <tr>
            <th v-for="col in columnsDisplay">{{col}}</th>
        </tr>
    </thead>
    <tbody>
        <tr v-for="row in rows">
            <td v-for="col in columns">{{row[col]}}</td>
        </tr>
    </tbody>
</table>